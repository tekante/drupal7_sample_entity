This is a Drupal profile for building a sample entity

Sample build

drush make se.make  /path/to/docroot
cp . /path/to/docroot/profiles/se

Then install Drupal using the sample entity profile

You'll need to add the navigation block to see navigation for listing and
managing the sample entity
